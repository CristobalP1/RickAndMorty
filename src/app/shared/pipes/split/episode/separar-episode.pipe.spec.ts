import { SepararEpisodePipe } from './separar-episode.pipe';

describe('SepararEpisodePipe', () => {
  it('create an instance', () => {
    const pipe = new SepararEpisodePipe();
    expect(pipe).toBeTruthy();
  });
});
