import { SepararSeriePipe } from './separar-serie.pipe';

describe('SepararSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new SepararSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
